/**
 * LanguageToggle — v1 stub
 *
 * 현재는 한국어 전용(Option C)이므로 실제 토글 UI 없음.
 * 향후 next-intl 도입(v2) 시 이 컴포넌트에 언어 전환 UI 추가 예정.
 */
export function LanguageToggle() {
  // v1: 한국어 고정, UI 없음
  return null;
}
