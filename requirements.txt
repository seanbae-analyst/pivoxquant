flask==3.0.3
flask-login==0.6.3
flask-sqlalchemy==3.1.1
flask-migrate>=4.1.0
flask-cors==4.0.1
pandas==2.2.2
numpy==1.26.4
apscheduler==3.10.4
werkzeug==3.1.8
requests==2.33.1
python-dotenv==1.0.1
anthropic>=0.93.0,<0.101.0
psycopg2-binary>=2.9.9
alpaca-py>=0.30.0
sentry-sdk[flask]>=2.59.0
authlib>=1.3.0
sendgrid>=6.12.5
# Brevo (Sendinblue) transactional v3 fallback: services/email/brevo_provider.py
# Uses requests directly (transitive dep) — no new SDK pin required.
# Free tier = 300 emails/day combined with SendGrid 100/day = 400/day budget.
# Honours feedback_no_extra_cost (zero new $cost).
stripe>=8.0.0
pywebpush>=2.0.0
flask-limiter>=3.5.0
cryptography>=42.0.0
# CA bundle for the CAUS cron — macOS/LibreSSL Python and headless cron
# environments can't always rely on the system trust store, so the sweep
# points every urllib call at certifi explicitly
# (scripts/caus_daily_sweep.py::_build_ssl_context).
certifi>=2024.7.4
gunicorn>=22.0.0
gevent>=24.2.1
# Kiwoom 영웅문 balance file parser (.xlsx path). .xls handled via optional
# xlrd fallback; CSV via stdlib. Keep openpyxl required so the /api/broker/
# kiwoom/csv endpoint never 500s on a fresh install.
openpyxl>=3.1.0
weasyprint>=62.0
jinja2>=3.1.6
# Monthly Brag Card (MVP #2) PNG rendering via headless Chromium.
# First run on a fresh host requires: `playwright install chromium`
# (downloads a ~120 MB browser bundle). The service tolerates a missing
# install: render_png() returns None and the Artifact row is still
# persisted for later rasterization.
playwright>=1.40
# Wave I D-2: Railway OOM/CPU pressure detector
# (scripts/nightly/railway_resource_check.py). psutil is already a
# transitive dep via sentry-sdk on some platforms, but pin explicitly
# so the resource probe never silently skips on a fresh container build.
psutil>=5.9.0
# Wave I E-2: WHOIS expiry monitor for pivoxquant.com
# (scripts/nightly/domain_expiry_check.py). python-whois wraps the
# raw WHOIS protocol (TCP 43) — no external API key, no $cost.
python-whois>=0.9.0
# 2026-09-01 — 직접 import 하는데 선언돼 있지 않던 두 패키지.
# 둘 다 지금은 전이 의존성으로 우연히 설치돼 있어 동작하지만, 그 우연이
# 깨지는 조건이 실재한다.
#
# websockets — services/kis/websocket_service.py 가 import 한다 (KR 실시간
#   시세 경로, services/data/realtime.py:453 에서 사용). 그런데 현재 이 패키지는
#   **alpaca-py 의 전이 의존성으로만** 들어온다. Alpaca 통합은 2026-05-27 에
#   제거됐고 게이트된 stub 만 남은 상태라, alpaca-py 를 정리하는 순간 KIS
#   웹소켓이 함께 죽는다. 게다가 import 가 try/except 로 감싸져 있어
#   ImportError 로 시끄럽게 실패하지 않고 WEBSOCKETS_OK=False 로 **조용히**
#   degrade 한다 — 그래서 더 찾기 어렵다.
websockets>=16.0
# itsdangerous — routes/auth.py 와 services/email_token.py 가 직접 import 한다
#   (서명 토큰). 현재는 Flask 의 전이 의존성이라 항상 설치되지만, 직접 쓰는
#   패키지는 직접 선언하는 편이 맞다.
itsdangerous>=2.2.0
