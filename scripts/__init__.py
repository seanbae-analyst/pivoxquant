"""Scripts package — enables `import scripts.<submodule>` from project root.

Required for `scripts.caus_scenarios.dayN` imports inside
`scripts/caus_daily_sweep.py` (Phase 3 Playwright scenarios).
"""
