# Bug Hunt — 2026-09-15 (Tue, overnight, autonomous)

**P0 2 · P1 8 · P2 6 · P3 5 — 수정 14 · 이월 7 · 커밋 1 (PR #584 브랜치)**

대상: Import Inbox (PR #583 + #584). 브랜치 `fix/import-order-notices` @ `f0bace1e` 위에서 작업.

## 방법 (측정 범위)

| 레그 | 도구 | 범위 |
|---|---|---|
| 실브라우저 | 앱 내 Browser + 로컬 `:5050`/`:3400`, dev-login | `/journal/import` 붙여넣기 → 결과 → 승인·거절, `/journal` 인박스, `/settings#import-tokens` 발급·폐기, `/portfolio` 반영, 웹훅 curl (text/plain·JSON rows) |
| `verify-security` 에이전트 | Flask test client + 코드 | IDOR, 웹훅 토큰·레이트리밋·CSRF 범위, CSV/XLSX(수식·zip bomb·cp949), ReDoS, 마스킹, 승인 레이스, 로그 PII, share_target XSS |
| `bug-hunter` 에이전트 | Flask test client | 브로커 알림 13종, CSV 13종, pending 라이프사이클, 토큰, 중복, 프론트 DTO 대조 |
| `engineering` 에이전트 | 전체 읽기 + 파서 단독 실행 | 원장 산식, 시간대, 트랜잭션, 마이그레이션 051/052, 프론트 계약·SWR, 중복 키, 주문알림 필터 회귀 |

파일 업로드 UI(드래그·파일 선택)는 브라우저 도구로 못 눌러 **API 레벨만** 검증. 실브라우저는 텍스트 경로·인박스·토큰 화면.

## 수정한 것 (이 브랜치)

| # | 심각도 | 결함 | 파일 |
|---|---|---|---|
| 1 | P0 | 웹훅 `rows[]`에 `traded_at` 없으면 `UnboundLocalError` → 500. 앞 행의 시간대 플래그가 뒤 행으로 새는 것도 같이 | `routes/imports.py` `_rows_to_raw` |
| 2 | P0 | 한글 종목명 + `$` 단가("애플 3주 매수 체결 $190.12") → KRW 로 저장 | `services/imports/text_parser.py` 통화 판정 순서 |
| 3 | P1 | 한투 해외주식 체결통보 어순("TSLA 매도 2주 …") → 종목명 "해외주식", 티커 없음 | `_US_CODE_FIRST`, `_US_CODE_QTY`, `_KEYWORDS` 복합어 |
| 4 | P1 | "118.5달러" 단가 미인식 → 체결 통째로 유실 | `_PRICE_DOLLAR_WORD` |
| 5 | P1 | 시각만 있는 알림("09:31 …")이 00:00 으로 저장돼 같은 날 두 번째 체결이 중복 판정 | `_date` 시각 전용 분기 |
| 6 | P1 | 주문알림 필터가 "미체결수량 0" · "(잔량/미체결 0주)" 가 붙은 **진짜 체결**을 버림 (PR #584 회귀) | `_UNFILLED_FIELD` 제거 후 판정 |
| 7 | P1 | CSV `통화=USD` 가 KRX 코드보다 우선 → ₩71,200 삼성전자가 $71,200. PATCH 도 동일, 잘못된 통화값은 조용히 무시 | `_currency_for`: 티커가 결정, PATCH 400 |
| 8 | P1 | 날짜 없는 체결의 "오늘"이 UTC 자정 → KST 00~09시엔 전날 | `services/imports/__init__.py::today_naive` KST 기준 |
| 9 | P1 | Sentry 가 기본으로 프레임 로컬 변수를 전송 → 미처리 예외 시 원본 Bearer 토큰·thesis 가 실릴 수 있음 | `app.py` `include_local_variables=False` |
| 10 | P2 | XLSX 압축 해제 크기 가드 없음 (2MB → 수 GB 팽창 가능) | `csv_parser.py::_guard_xlsx_size` 32MB·512 entry |
| 11 | P2 | 무료 티어 종목 캡 검사에 User 행 잠금 없음 (수동 경로 Bug C#2 와 동일 TOCTOU) | `ledger.py::_enforce_position_cap` FOR UPDATE |
| 12 | P2 | CSV 에서 파서가 1건이라도 건너뛰면 sanity 패스의 건너뜀(범위·미래일자)이 응답에서 사라짐; 행 번호도 두 경로가 달랐음 | `_ingest` skipped 병합(행 번호 기준 중복 제거) |
| 13 | P2 | 백엔드가 주는 `skipped[]` 를 프론트가 표시하지 않음 — 4줄 중 2줄이 버려져도 "파싱 2건"만 보임 | `types.ts` `ImportSkippedRow`, `journal/import/page.tsx` 목록 렌더 |
| 14 | P2 | 날짜 없는 체결이 "오전 09:00" 으로 표시돼 실제 시각처럼 보임(`confidence` 미표시) | `import-inbox.tsx` "시각 추정" 배지 + 툴팁 |
| 15 | P2 | 승인 후 pending 키만 갱신 → `/portfolio`·거울 5종·멈춤 목록이 세션 내내 stale | `import-inbox.tsx::refreshBookAfterApprove` |
| 16 | P2 | 가져오기 페이지·토큰 발급의 **동의 문구**가 "원본 텍스트 저장 안 함" 이라고 함 — `raw_snippet` 300자가 남음(랜딩 a7 과 같은 오류) | `ko.json`/`en.json` consentLabel 2곳 |
| 17 | P3 | 웹훅 데코레이터 순서상 토큰 401 경로엔 라우트 레이트리밋이 안 걸림 | `general_rate_limit` 를 바깥으로 |
| 18 | P3 | 429 응답의 코드·한국어 메시지를 `api.ts` 가 버림 → `IMPORT_TOKEN_DAILY_LIMIT` 도달 불가 | `api.ts` 429 본문 파싱 |
| 19 | P3 | "KODEX 200 20주 … 35,000원" 종목명이 "000원" | `_kr_name` 천단위 콤마 제거, `_leading_name` 폴백 |
| 20 | P3 | 단가 라벨에 `at` 없음("filled at 120.50") | `_PRICE_LABELLED` |

회귀 테스트: `tests/test_imports_route.py::TestHunt20260915` 14개.

## 이월 (수정 안 함 — 근거)

| 심각도 | 결함 | 왜 미룸 |
|---|---|---|
| P2 | `duplicate` 상태 행이 영구 종단 — 같은 분·같은 단가의 분할체결 두 번째가 조용히 사라짐 (`dedupe.py` 키가 분 단위) | 키 의미 변경은 기존 테스트(`test_within_batch_duplicate`)와 제품 결정이 걸림. 최소안: duplicate 행 거절 허용 + `/pending?status=` |
| P2 | 원장이 과거 체결에도 **오늘** 환율 사용(`ledger.py:83,105`), FX 실패 시 `buy_fx_rate=0` 저장 가능 | 과거 환율 소스 필요. Pattern 7 영역이라 낮에 결정 |
| P2 | CSV 경로에 주문상태(체결여부·주문상태) 열 필터 없음 — 주문내역 파일의 취소·미체결 행이 체결로 들어옴 | 헤더 동의어 + 상태 어휘 추가 필요, 브로커 샘플 없이 밤에 못 검증 |
| P2 | `legal_filter.py:199` `\bMust\b` → "is recorded as" 규칙이 "must be" 검증 문구를 전부 깨뜨림("name is recorded as be 1–60 characters") | CLAUDE.md 함정 4 · 원칙: `legal_filter.py` 는 P0 법적 위반 없이는 손대지 않음. 낮에 규칙 범위만 좁힐 것 |
| P2 | `raw_snippet` 이 이름("홍길동님")을 마스킹하지 않음, 평문 컬럼 | 이름 휴리스틱은 오탐 위험, 암호화는 마이그레이션 필요 |
| P3 | `(user_id,status)`·`(user_id,dedupe_key)` 복합 인덱스 없음, `token_hash` 제약 형태가 모델/마이그레이션 간 다름 | 마이그레이션 동반 |
| P3 | `/pending` 200건 절단·배치 내 역순, 2000행 CSV 절단 무음, `import-inbox.tsx` 승인 버튼 이중 클릭 창, 멀티파트에 `file` 없을 때 오류 코드 오표시 | 각각 작음, 묶어서 다음 스윕 |

## 검증

| 항목 | 결과 |
|---|---|
| `tests/test_imports_route.py` + `test_import_tokens_route.py` | 80 passed (신규 14 포함) |
| 백엔드 전체 pytest | 2302 passed · 18 skipped · 1 xfailed (6m18s) |
| ruff (routes/imports, services/imports, app.py, tests) | clean |
| vitest / tsc / eslint | 414 passed / clean / clean |
| 실브라우저 재확인 (백엔드 재기동 후) | TSLA·NVDA 118.5달러·애플 $ 세 줄 모두 USD 티커로 대기 목록 진입, 접수 알림은 `skipped` 로 응답; 결과 패널에 건너뛴 줄 목록·"시각 추정" 배지 렌더 확인 |

## 그 외 관찰

- 로컬 `/portfolio` 는 KIS 시세 지연 시 "Loading positions…" 가 8초 넘게 남는다. 시세와 무관한 보유 표를 시세 응답에 묶어둔 구조. 버그로 올리지 않음(조건부 재현).
- 동의 체크 없이 "읽어오기" 를 누르면 아무 피드백이 없다(버튼만 비활성). P3, 이월.
- 에이전트 셋 다 저장소를 수정하지 않았음을 `git status` 로 확인.

<!-- bug-hunt:complete -->
